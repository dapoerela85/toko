title               : "Dapoer Ela 85"
description         : ""
baseurl             : "/toko/"
keywords            : ""
url                 : "https://dapoerela85.github.io/toko/"
locale              : "id-ID"
github_username     : dapoerela85
permalink           : pretty
markdown            : kramdown
paginate            : 4
paginate_path       : "/toko/:num/"

include:
  - _pages

plugins:
  - jekyll-feed
  - jekyll-paginate
  - jekyll-sitemap

defaults:
  - scope:
      path: "_posts/**"
      type: posts
    values:
      layout: post
      permalink: /product/:title/
  - scope:
      path: "_pages"
      type: "pages"
    values:
      layout: page
